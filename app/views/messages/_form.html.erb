<%= form_with(model: [post, chat_room, message], local: false, data: { controller: "reset-form", action: "turbo:submit-end->reset-form#reset" }) do |form| %>
  <div class="flex items-end space-x-2">
    <div class="flex-1">
      <%= form.text_area :content_raw, 
          rows: 1,
          placeholder: "Nhập tin nhắn...",
          class: "w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent resize-none",
          data: { action: "input->message-input#adjustHeight keydown.enter->message-form#submitOnEnter" },
          required: true %>
    </div>
    
    <%= form.submit "Gửi", class: "px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors font-medium disabled:opacity-50 disabled:cursor-not-allowed" %>
  </div>
  
  <% if chat_room.buyer.locale != chat_room.seller.locale %>
    <p class="text-xs text-gray-500 mt-2">
      <svg class="inline w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
      </svg>
      Tin nhắn sẽ được tự động dịch
    </p>
  <% end %>
<% end %>