<div class="container mx-auto px-0 md:px-4 py-0 md:py-6 max-w-2xl">
  <div class="bg-white md:rounded-lg md:shadow-sm p-6">
    <h1 class="text-xl md:text-2xl font-bold text-gray-900 mb-4 md:mb-6">
      <%= @post.draft? ? t("posts.edit_draft") : t("posts.edit") %>
    </h1>
    
    <%= render "form", post: @post %>
  </div>
</div>
