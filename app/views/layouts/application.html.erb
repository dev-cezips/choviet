<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Cho Viet" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Google Font: Be Vietnam Pro -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@400;500;700&display=swap" rel="stylesheet">
    
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    
    <style>
      /* Force white background */
      html, body {
        background-color: #ffffff !important;
        background: #ffffff !important;
      }

      /* Ensure main content is white */
      main, .bg-off-white, .bg-gray-50 {
        background-color: #ffffff !important;
      }

    </style>
  </head>

  <body class="font-vietnam" style="<%= turbo_native_app? ? 'padding-top: env(safe-area-inset-top, 0px);' : 'padding-top: 120px !important;' %> background-color: #ffffff !important;">
    <% unless turbo_native_app? %>
      <%= render "layouts/header" %>
    <% end %>

    <!-- Main content wrapper -->
    <div>
      <%= turbo_frame_tag "flash" do %>
        <%= render "shared/flash" %>
      <% end %>

      <!-- Main content -->
      <main>
        <%= yield %>
      </main>
    </div>
    
    <!-- Footer -->
    <footer class="bg-gray-100 border-t border-gray-200 mt-20">
      <div class="max-w-7xl mx-auto px-4 py-8">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <!-- About -->
          <div>
            <h3 class="text-lg font-semibold mb-4"><%= t('footer.about.title') %></h3>
            <p class="text-sm text-gray-600">
              <%= t('footer.about.description') %>
            </p>
          </div>
          
          <!-- Links -->
          <div>
            <h3 class="text-lg font-semibold mb-4"><%= t('footer.links.title') %></h3>
            <ul class="space-y-2">
              <li>
                <%= link_to t('footer.links.privacy'), privacy_path, class: "text-sm text-gray-600 hover:text-gray-900" %>
              </li>
              <li>
                <%= link_to t('footer.links.terms'), terms_path, class: "text-sm text-gray-600 hover:text-gray-900" %>
              </li>
            </ul>
          </div>
          
          <!-- Contact -->
          <div>
            <h3 class="text-lg font-semibold mb-4"><%= t('footer.contact.title') %></h3>
            <p class="text-sm text-gray-600">
              <%= t('footer.contact.email') %><br>
              <%= t('footer.contact.kakao') %>
            </p>
          </div>
        </div>
        
        <div class="border-t border-gray-200 mt-8 pt-8 text-center">
          <p class="text-sm text-gray-500">
            <%= t('footer.copyright', year: Date.current.year).html_safe %>
          </p>
        </div>
      </div>
    </footer>
    
    <!-- Modal frames (modal = new standard, report_modal = legacy compat) -->
    <%= turbo_frame_tag "modal" %>
    <%= turbo_frame_tag "report_modal" %>
    
    <%= render "shared/rate_limit_notice" if defined?(request) %>
    
    <!-- Floating Write Button (hidden on report pages and in Turbo Native) -->
    <% if user_signed_in? && !turbo_native_app? && !controller_name.in?(['reports']) %>
      <div class="fixed bottom-6 right-6 z-40 md:hidden">
        <%= link_to new_post_path, class: "group flex items-center justify-center w-14 h-14 bg-blue-600 text-white rounded-full shadow-lg hover:bg-blue-700 hover:shadow-xl transition-all duration-200 hover:scale-110" do %>
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
          </svg>
          <span class="absolute right-full mr-3 py-1 px-2 bg-gray-900 text-white text-sm rounded opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap">
            <%= t('fab.new_post') %>
          </span>
        <% end %>
      </div>
    <% elsif !user_signed_in? && !devise_controller? %>
      <% if turbo_native_app? %>
        <!-- Turbo Native: Clear login bar at bottom -->
        <div class="fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 px-4 py-3 z-40" style="padding-bottom: env(safe-area-inset-bottom, 12px);">
          <div class="flex items-center justify-between max-w-screen-lg mx-auto">
            <span class="text-sm text-gray-600"><%= t('auth.login_prompt', default: 'Đăng nhập để đăng bài') %></span>
            <div class="flex gap-2">
              <%= link_to t('nav.sign_in'), new_user_session_path, class: "px-4 py-2 text-sm font-medium text-blue-600 hover:text-blue-700" %>
              <%= link_to t('nav.sign_up'), new_user_registration_path, class: "px-4 py-2 text-sm font-medium text-white rounded-lg", style: "background-color: #0068FF;" %>
            </div>
          </div>
        </div>
      <% else %>
        <div class="fixed bottom-6 right-6 z-40 md:hidden">
          <%= link_to new_user_session_path, class: "group flex items-center justify-center w-14 h-14 bg-blue-600 text-white rounded-full shadow-lg hover:bg-blue-700 hover:shadow-xl transition-all duration-200 hover:scale-110" do %>
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
            </svg>
            <span class="absolute right-full mr-3 py-1 px-2 bg-gray-900 text-white text-sm rounded opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap">
              <%= t('fab.login_to_post') %>
            </span>
          <% end %>
        </div>
      <% end %>
    <% end %>
  </body>
</html>
