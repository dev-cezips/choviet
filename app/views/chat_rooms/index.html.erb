<div class="bg-gray-50 min-h-screen">
  <div class="container mx-auto px-4 py-6 max-w-4xl">
    <!-- Header -->
    <div class="mb-6">
      <h1 class="text-2xl font-bold text-gray-900">Tin nhắn</h1>
      <p class="text-gray-500 text-sm mt-1">Tất cả cuộc trò chuyện của bạn</p>
    </div>
    
    <% if @chat_rooms.any? %>
      <div class="bg-white rounded-lg shadow-sm divide-y divide-gray-100">
        <% @chat_rooms.each do |chat_room| %>
          <% other_user = chat_room.other_user(current_user) %>
          <% last_message = chat_room.last_message %>
          <% unread_count = chat_room.unread_count_for(current_user) %>
          
          <%= link_to post_chat_room_path(chat_room.post, chat_room), 
              class: "block px-4 py-4 hover:bg-gray-50 transition-colors #{'bg-blue-50' if unread_count > 0}" do %>
            <div class="flex items-start gap-3">
              <!-- Avatar -->
              <div class="flex-shrink-0">
                <%= user_avatar(other_user, size: "w-12 h-12", text_size: "text-lg") %>
              </div>
              
              <!-- Content -->
              <div class="flex-1 min-w-0">
                <div class="flex items-start justify-between mb-1">
                  <h3 class="font-semibold text-gray-900 truncate">
                    <%= other_user.display_name %>
                  </h3>
                  <span class="text-xs text-gray-500 whitespace-nowrap ml-2">
                    <%= viet_time_ago(last_message&.created_at || chat_room.created_at) %>
                  </span>
                </div>
                
                <!-- Post title -->
                <p class="text-xs text-gray-600 mb-1 truncate">
                  <%= post_type_icon(chat_room.post) %> <%= chat_room.post.title %>
                </p>
                
                <!-- Last message -->
                <div class="flex items-center justify-between">
                  <p class="text-sm text-gray-700 truncate flex-1">
                    <% if last_message %>
                      <% if last_message.sender_id == current_user.id %>
                        <span class="text-gray-500">Bạn: </span>
                      <% end %>
                      <%= truncate(last_message.display_content, length: 50) %>
                    <% else %>
                      <span class="text-gray-400 italic">Chưa có tin nhắn</span>
                    <% end %>
                  </p>
                  
                  <% if unread_count > 0 %>
                    <%= render "shared/counter_badge",
                        count: unread_count,
                        class_name: "ml-2 bg-blue-600 text-white" %>
                  <% end %>
                </div>
              </div>
              
              <!-- Post image -->
              <% if chat_room.post.images.attached? %>
                <div class="flex-shrink-0">
                  <%= image_tag chat_room.post.images.first, 
                      class: "w-16 h-16 object-cover rounded-lg" %>
                </div>
              <% end %>
            </div>
          <% end %>
        <% end %>
      </div>
    <% else %>
      <!-- Empty State -->
      <div class="bg-white rounded-lg shadow-sm p-12 text-center">
        <svg class="w-24 h-24 text-gray-300 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
        </svg>
        <h3 class="text-lg font-medium text-gray-900 mb-2">Chưa có tin nhắn nào</h3>
        <p class="text-gray-500 mb-6">
          Bắt đầu trò chuyện khi bạn quan tâm đến một sản phẩm
        </p>
        <%= link_to "Khám phá sản phẩm", posts_path(type: 'marketplace'), 
            class: "inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors" %>
      </div>
    <% end %>
  </div>
</div>